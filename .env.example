# API Backend URL
# For local development, use: http://localhost:3001/api
# For production, use your Render backend URL: https://your-backend-url.onrender.com/api
# Note: The /api suffix is automatically added if not present, so you can also use just the base URL
VITE_API_URL=http://localhost:3001/api
