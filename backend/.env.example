# Database Configuration
DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# JWT Secret Key (change this in production!)
JWT_SECRET=your-secret-key-here-change-in-production

# Server Configuration
PORT=3001
NODE_ENV=production

# Frontend URL (for CORS)
# For multiple URLs, separate with commas:
# FRONTEND_URL=https://app1.onrender.com,https://app2.onrender.com
# 
# Note: If not set in production, the API will allow ecobuddy-*.onrender.com domains as a fallback
# For maximum security, always set this explicitly to your frontend URL(s)
FRONTEND_URL=https://your-frontend-url.onrender.com
